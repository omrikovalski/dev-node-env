#!/bin/bash
docker cp "$(docker-compose -f ./dc/docker-compose.yml ps -q client|awk '{print $1}')":/app/node_modules/./ "./client/node_modules/"
echo "Completed copying ✂️  all files from container to host"