#!/bin/bash
docker cp "$(docker-compose -f ./dc/docker-compose.yml ps -q middleware|awk '{print $1}')":/usr/src/app/node_modules/./ "./middleware/node_modules/"
echo "Completed copying ✂️  all files from container to host"